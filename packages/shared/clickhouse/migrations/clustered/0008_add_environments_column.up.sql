ALTER TABLE traces ON CLUSTER {CLICKHOUSE_CLUSTER_NAME} ADD COLUMN environment LowCardinality(String) DEFAULT 'default' AFTER project_id SETTINGS alter_sync = 2;
ALTER TABLE observations ON CLUSTER {CLICKHOUSE_CLUSTER_NAME} ADD COLUMN environment LowCardinality(String) DEFAULT 'default' AFTER project_id SETTINGS alter_sync = 2;
ALTER TABLE scores ON CLUSTER {CLICKHOUSE_CLUSTER_NAME} ADD COLUMN environment LowCardinality(String) DEFAULT 'default' AFTER project_id SETTINGS alter_sync = 2;
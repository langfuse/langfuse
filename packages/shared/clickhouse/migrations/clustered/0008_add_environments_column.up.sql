ALTER TABLE traces ON CLUSTER ${CLICKHOUSE_CLUSTER_NAME} ADD COLUMN environment LowCardinality(String) DEFAULT 'default' AFTER project_id;
ALTER TABLE observations ON CLUSTER ${CLICKHOUSE_CLUSTER_NAME} ADD COLUMN environment LowCardinality(String) DEFAULT 'default' AFTER project_id;
ALTER TABLE scores ON CLUSTER ${CLICKHOUSE_CLUSTER_NAME} ADD COLUMN environment LowCardinality(String) DEFAULT 'default' AFTER project_id;
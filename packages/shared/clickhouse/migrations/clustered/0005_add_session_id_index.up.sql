ALTER TABLE traces ON CLUSTER ${CLICKHOUSE_CLUSTER_NAME} ADD INDEX IF NOT EXISTS idx_session_id session_id TYPE bloom_filter() GRANULARITY 1;
ALTER TABLE traces ON CLUSTER ${CLICKHOUSE_CLUSTER_NAME} MATERIALIZE INDEX IF EXISTS idx_session_id;
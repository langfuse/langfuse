{
  "name": "@langfuse/native",
  "version": "0.0.0",
  "description": "Rust native addon (napi-rs) loaded by the Langfuse worker",
  "license": "MIT",
  "private": true,
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "index.js",
    "index.d.ts",
    "*.node"
  ],
  "engines": {
    "node": "24"
  },
  "scripts": {
    "prepare-clickhouse-rs": "sh scripts/prepare-clickhouse-rs.sh",
    "build": "pnpm run rust:build",
    "rust:build": "pnpm run prepare-clickhouse-rs && napi build --platform --release",
    "build:debug": "pnpm run rust:build:debug",
    "rust:build:debug": "pnpm run prepare-clickhouse-rs && napi build --platform",
    "lint": "pnpm run rust:lint",
    "rust:lint": "pnpm run prepare-clickhouse-rs && cargo fmt --check && cargo clippy --all-targets -- -D warnings",
    "lint:fix": "pnpm run rust:lint:fix",
    "rust:lint:fix": "pnpm run prepare-clickhouse-rs && cargo fmt && cargo clippy --all-targets --fix --allow-dirty -- -D warnings",
    "test": "pnpm run rust:test",
    "rust:test": "pnpm run prepare-clickhouse-rs && cargo test --locked"
  },
  "napi": {
    "binaryName": "langfuse-native",
    "targets": [
      "x86_64-unknown-linux-musl",
      "aarch64-unknown-linux-musl",
      "x86_64-unknown-linux-gnu",
      "aarch64-unknown-linux-gnu",
      "aarch64-apple-darwin",
      "x86_64-apple-darwin"
    ]
  },
  "devDependencies": {
    "@napi-rs/cli": "3.8.6"
  }
}

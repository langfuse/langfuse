{
  "name": "langfuse",
  "version": "2.7.0",
  "scripts": {
    "infra:dev:up": "docker-compose -f ./docker-compose.dev.yml up -d",
    "infra:dev:down": "docker-compose -f ./docker-compose.dev.yml down",
    "dev": "npm run dev --workspace=worker & npm run dev --workspace=web",
    "nuke": "find . -name 'node_modules' -type d -prune -print -exec rm -rf '{}' \\; && find . -name '.next' -type d -prune -print -exec rm -rf '{}' \\;",
    "dx": "dotenv -e .env -- npm i && npm run infra:dev:up && cd web && npm run db:reset && npm run db:seed:examples && cd .. && npm run dev"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/eslint": "^8.56.2",
    "@types/jest": "^29.5.12",
    "@typescript-eslint/eslint-plugin": "^7.0.2",
    "@typescript-eslint/parser": "^7.0.2",
    "dotenv-cli": "^7.3.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.5"
  },
  "workspaces": [
    "worker",
    "web",
    "shared"
  ]
}

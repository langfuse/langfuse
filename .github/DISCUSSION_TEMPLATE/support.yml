name: Question
description: Ask a question about Langfuse
title: "[Question]: "
labels: ["question"]

body:
  - type: textarea
    id: question
    attributes:
      label: Describe your question
      description: Please describe your question as clearly and concisely as possible.
    validations:
      required: true

  - type: dropdown
    id: hosting
    attributes:
      label: Are you using Langfuse Cloud or self-hosting?
      options:
        - Langfuse Cloud
        - Self-hosted Langfuse
    validations:
      required: true

  - type: input
    id: sdk-versions
    attributes:
      label: Langfuse SDK version(s)
      description: List the version numbers of the Langfuse SDK(s) you are using (e.g., `langfuse-js@2.0.1`, `langfuse-python@1.9.0`).
      placeholder: "langfuse-js@x.y.z, langfuse-python@x.y.z"
    validations:
      required: true

  - type: input
    id: selfhost-version
    attributes:
      label: Self-hosted Langfuse version
      description: If you are self-hosting Langfuse, provide the version number you are running. If you are using Langfuse Cloud, you can leave this blank.
      placeholder: "e.g. v2.19.1"
    validations:
      required: false

  - type: checkboxes
    id: confirmations
    attributes:
      label: Before submitting
      description: |
        Please confirm you've done the following:
        - Checked for an existing [issue](https://github.com/langfuse/langfuse/issues)
        - Searched existing [discussions](https://github.com/orgs/langfuse/discussions)
        - Asked the [Langfuse AI](https://langfuse.com/docs/ask-ai) for help
      options:
        - label: I checked existing issues.
          required: true
        - label: I searched existing discussions.
        - label: I asked the Langfuse AI for help.
    validations:
      required: true

# Preview pipeline smoke test

Throwaway PR to validate the AWS preview deployment pipeline end-to-end
(build → comment → Argo deploy → login → cookie namespacing → consolidation).
Do NOT merge — close after verification.

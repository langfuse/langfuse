/** Viewport-fixed overlays (the table peek) read this to sit left of a docked
 * right rail instead of painting over it. Published while a RightRail is
 * mounted; removed on unmount. */
export const APP_RIGHT_RAIL_WIDTH_VAR = "--app-right-rail-width";

// The slack feature's server surface (RFC rule 9, amended): the webhook
// helper other server features already imported by file path.
export { sendToSlack } from "@/src/features/slack/server/slack-webhook";

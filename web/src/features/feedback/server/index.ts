// The feedback feature's server surface (RFC rule 9, amended): the
// product-feedback helpers MCP already imported by file path.
export {
  isProductFeedbackAvailable,
  submitFeedback,
} from "@/src/features/feedback/server/FeedbackService";

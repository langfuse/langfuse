// The table feature's public server surface (RFC rules 8 and 10).
// tableRouter stays a direct import from the tRPC root, which is not a feature.
export { createBatchActionJob } from "@/src/features/table/server/createBatchActionJob";

// The sdk-version feature's server surface (RFC rule 9, amended): the
// capability helper server callers already imported by file path.
export { getSdkVersionCapabilityStatus } from "@/src/features/sdk-version/lib/sdkVersionCapabilities";

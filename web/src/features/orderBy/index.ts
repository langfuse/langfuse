// The orderBy feature's public client surface (RFC rule 8). Named
// re-exports only — the table sort hook other features already imported
// by file path.
export { useOrderByState } from "@/src/features/orderBy/hooks/useOrderByState";

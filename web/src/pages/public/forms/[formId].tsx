// CIP fork route (see FORK.md). Lives under /public/ so the app layout
// renders it without navigation and without an auth redirect.
import PublicForm from "@/src/features/cip-elicitations/pages/PublicForm";

export default PublicForm;

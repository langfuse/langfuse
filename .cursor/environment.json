{
  "install": "cp .env.dev.example .env && pnpm i",
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  },
  "terminals": [
    {
      "name": "Run dev server",
      "command": "pnpm dx-f"
    }
  ]
}

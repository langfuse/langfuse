{
  "name": "ai-gateway",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ../.env -v LANGFUSE_AI_GATEWAY_AUTO_INCREMENT_LISTEN_PORT=true -- bash scripts/run-dev.sh",
    "build": "pnpm run rust:build",
    "rust:build": "cargo build --release --locked",
    "build:dev": "pnpm run rust:build:dev",
    "rust:build:dev": "cargo build --locked",
    "typecheck": "pnpm run rust:typecheck",
    "rust:typecheck": "cargo check --locked --all-targets",
    "lint": "pnpm run rust:lint",
    "rust:lint": "cargo fmt --all -- --check && cargo clippy --locked --all-targets -- -D warnings",
    "test": "pnpm run rust:test",
    "rust:test": "cargo test --locked",
    "format": "cargo fmt --all"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.4"
  }
}

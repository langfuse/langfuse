# yaml-language-server: $schema=https://raw.githubusercontent.com/fern-api/fern/main/fern.schema.json

service:
  auth: false
  base-path: /api
  endpoints:
    create:
      docs: Add a trace to the database
      method: POST
      path: /traces
      request: CreateTraceRequest
      response: Trace
    update:
      docs: Update the status of a trace
      method: PATCH
      path: /traces
      request: UpdateTraceRequest
      response: Trace

types:
  CreateTraceRequest:
    properties:
      name: string
      attributes: unknown
      status: string
      statusMessage: optional<string>
  UpdateTraceRequest:
    properties:
      traceId: string
      status: string
      statusMessage: optional<string>
  Trace:
    properties:
      id: string
      timestamp: datetime
      name: string
      attributes: unknown
      status: string
      statusMessage: optional<string>

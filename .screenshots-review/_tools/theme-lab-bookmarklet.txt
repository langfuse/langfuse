javascript:(function () { "use strict"; var PANEL_ID = "theme-lab-panel"; var LS_KEY = "themeLabOverrides"; var TYPO_STYLE_ID = "theme-lab-typography"; var PRECONNECT_ID = "theme-lab-fonts-preconnect"; var PLEX_SANS_CSS_ID = "theme-lab-plex-sans-css"; var PLEX_SANS_URL = "https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600&display=swap"; var PLEX_MONO_CSS_ID = "theme-lab-plex-mono-css"; var PLEX_MONO_URL = "https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&display=swap"; var INTER_CSS_ID = "theme-lab-inter-css"; var INTER_URL = "https://fonts.googleapis.com/css2?family=Inter:wght@400..700&display=swap"; var PAIRINGS = [ { val: "default", label: "App default", bold: null }, { val: "inter", label: "Inter", bold: "600" }, { val: "plex", label: "IBM Plex", bold: "500" }, ]; var PCT = String.fromCharCode( 37, ); var CO = "/" + "*"; var CC = "*" + "/"; var root = document.documentElement; var existing = document.getElementById(PANEL_ID); if (existing) { existing.style.display = ""; existing.style.boxShadow = "0 0 0 2px rgb(150,140,255), -8px 0 28px rgba(0,0,0,0.4)"; setTimeout(function () { existing.style.boxShadow = "-8px 0 28px rgba(0,0,0,0.4)"; }, 600); return; } var TIERS = [ { key: "chrome", label: "chrome", props: ["--header", "--sidebar-background"], }, { key: "canvas", label: "canvas", props: ["--background"] }, { key: "card", label: "card", props: ["--card"] }, { key: "modal", label: "modal", props: ["--modal"] }, { key: "popover", label: "popover", props: ["--popover"] }, { key: "border", label: "hairline", props: ["--border"] }, ]; var EXTRAS = [ { key: "sbBorder", label: "sb-border", props: ["--sidebar-border"] }, { key: "sbAccent", label: "sb-accent", props: ["--sidebar-accent"] }, { key: "code", label: "code", props: ["--surface-code"] }, { key: "muted", label: "muted", props: ["--muted"] }, { key: "accent", label: "accent", props: ["--accent"] }, ]; var ALL = TIERS.concat(EXTRAS); var TEXT_TIERS = [ { key: "body", label: "body", prop: "--foreground", vs: "--background" }, { key: "meta", label: "meta", prop: "--muted-foreground", vs: "--background" }, { key: "faint", label: "faint", prop: "--foreground-tertiary", vs: "--background" }, { key: "sidebar", label: "sidebar", prop: "--sidebar-foreground", vs: "--sidebar-background" }, { key: "link", label: "link", prop: "--link", vs: "--background" }, { key: "linkHover", label: "link-hover", prop: "--link-hover", vs: "--background" }, ]; var VS_LADDER_KEY = { "--background": "canvas", "--sidebar-background": "chrome" }; var ACCENT_TIERS = [ { key: "primaryAccent", label: "primary", prop: "--primary-accent", vsProp: "--background" }, { key: "accentFill", label: "accent", prop: "--accent", vsProp: null }, { key: "accentFg", label: "accent-fg", prop: "--accent-foreground", vsProp: "--accent" }, { key: "ring", label: "ring", prop: "--ring", vsProp: "--background" }, { key: "sbAccentFill", label: "sb-accent", prop: "--sidebar-accent", vsProp: null }, { key: "sbAccentFg", label: "sb-acc-fg", prop: "--sidebar-accent-foreground", vsProp: "--sidebar-accent" }, ]; var MODE = root.classList.contains("dark") ? "dark" : "light"; var TYPE_TOKENS = [ { key: "xs", def: 0.7 }, { key: "sm", def: 0.825 }, { key: "base", def: 0.9 }, { key: "lg", def: 1.1 }, { key: "xl", def: 1.2 }, { key: "2xl", def: 1.3 }, { key: "3xl", def: 1.5 }, ]; var PRESETS = [ { name: "v1", vals: { chrome: 2, canvas: 4.5, card: 7, modal: 9.5, popover: 12, border: 15, }, }, { name: "v2 (current)", vals: { chrome: 2, canvas: 6, card: 7.5, modal: 9, popover: 12, border: 15, }, }, { name: "pre-PR", vals: { chrome: 6.3, canvas: 3.5, card: 8.2, modal: 3.5, popover: 10.2, border: 10.5, }, }, ]; function fmtNum(n) { return String(Math.round(n * 10) / 10); } function fmtRem(n) { return String(parseFloat(n.toFixed(4))); } function fmtPx(rem) { return String(parseFloat((rem * 16).toFixed(2))); } function parseTriplet(v) { var parts = (v || "").trim().split(/\s+/); if (parts.length >= 3) { var l = parseFloat(parts[parts.length - 1]); if (isFinite(l)) return { h: parts[0], s: parts[1], l: l }; } return { h: "0", s: "0" + PCT, l: 0 }; } var baseline = {}; function captureBaseline(prop) { var inline = root.style.getPropertyValue(prop); if (inline) root.style.removeProperty(prop); baseline[prop] = parseTriplet(getComputedStyle(root).getPropertyValue(prop)); if (inline) root.style.setProperty(prop, inline); } ALL.forEach(function (tier) { tier.props.forEach(captureBaseline); }); TEXT_TIERS.forEach(function (t) { captureBaseline(t.prop); }); ACCENT_TIERS.forEach(function (t) { captureBaseline(t.prop); }); function tripletFor(prop, l) { var b = baseline[prop]; return b.h + " " + b.s + " " + fmtNum(l) + PCT; } var state = {}; ALL.forEach(function (t) { state[t.key] = baseline[t.props[0]].l; }); var WEIGHTS = [400, 450, 500, 600, 700]; var typo = { pairing: "default", sizes: {}, weights: {} }; TYPE_TOKENS.forEach(function (t) { typo.sizes[t.key] = t.def; typo.weights[t.key] = null; }); var textState = {}; TEXT_TIERS.forEach(function (t) { textState[t.key] = baseline[t.prop].l; }); var textColorsStore = { dark: {}, light: {} }; function baselineHsl(prop) { var b = baseline[prop]; return { h: parseFloat(b.h) || 0, s: parseFloat(b.s) || 0, l: b.l }; } var accentState = {}; ACCENT_TIERS.forEach(function (t) { accentState[t.key] = baselineHsl(t.prop); }); var accentColorsStore = { dark: {}, light: {} }; var UI_DEFAULT_WIDTH = 320; var UI_MIN_WIDTH = 260; var UI_MAX_WIDTH = 560; var ui = { width: UI_DEFAULT_WIDTH, collapsed: false }; function applyTier(t) { t.props.forEach(function (prop) { root.style.setProperty(prop, tripletFor(prop, state[t.key])); }); } function isChanged(t) { return Math.abs(state[t.key] - baseline[t.props[0]].l) > 0.001; } function typoSizeChanged(t) { return Math.abs(typo.sizes[t.key] - t.def) > 0.001; } function typoWeightChanged(t) { return typo.weights[t.key] != null; } function typoRowChanged(t) { return typoSizeChanged(t) || typoWeightChanged(t); } function applyTextTier(t) { root.style.setProperty(t.prop, tripletFor(t.prop, textState[t.key])); } function isTextChanged(t) { return Math.abs(textState[t.key] - baseline[t.prop].l) > 0.001; } function mod(n, m) { return n - m * Math.floor(n / m); } function hslToRgb(h, s, l) { s = s / 100; l = l / 100; var c = (1 - Math.abs(2 * l - 1)) * s; var hp = mod(h, 360) / 60; var x = c * (1 - Math.abs(mod(hp, 2) - 1)); var r = 0; var g = 0; var b = 0; if (hp < 1) { r = c; g = x; } else if (hp < 2) { r = x; g = c; } else if (hp < 3) { g = c; b = x; } else if (hp < 4) { g = x; b = c; } else if (hp < 5) { r = x; b = c; } else { r = c; b = x; } var m = l - c / 2; return [r + m, g + m, b + m]; } function relLum(rgb) { var a = rgb.map(function (v) { return v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4); }); return 0.2126 * a[0] + 0.7152 * a[1] + 0.0722 * a[2]; } function contrastRatio(t1, t2) { var l1 = relLum(hslToRgb(t1.h, t1.s, t1.l)); var l2 = relLum(hslToRgb(t2.h, t2.s, t2.l)); var hi = Math.max(l1, l2); var lo = Math.min(l1, l2); return (hi + 0.05) / (lo + 0.05); } function hslOfProp(prop, l) { var b = baseline[prop]; return { h: parseFloat(b.h) || 0, s: parseFloat(b.s) || 0, l: l }; } function textContrast(t) { var fg = hslOfProp(t.prop, textState[t.key]); var bg = hslOfProp(t.vs, state[VS_LADDER_KEY[t.vs]]); return contrastRatio(fg, bg); } function hslTripletStr(a) { return fmtNum(a.h) + " " + fmtNum(a.s) + PCT + " " + fmtNum(a.l) + PCT; } function accentTripletStr(t) { return hslTripletStr(accentState[t.key]); } function applyAccent(t) { root.style.setProperty(t.prop, accentTripletStr(t)); } function isAccentChanged(t) { var b = baselineHsl(t.prop); var a = accentState[t.key]; return ( Math.abs(a.h - b.h) > 0.001 || Math.abs(a.s - b.s) > 0.001 || Math.abs(a.l - b.l) > 0.001 ); } function computedHsl(prop) { var tr = parseTriplet(getComputedStyle(root).getPropertyValue(prop)); return { h: parseFloat(tr.h) || 0, s: parseFloat(tr.s) || 0, l: tr.l }; } var HASH = String.fromCharCode(35); function hslToHex(h, s, l) { var rgb = hslToRgb(h, s, l); var out = HASH; for (var i = 0; i < 3; i++) { var v = Math.round(Math.min(1, Math.max(0, rgb[i])) * 255); var hx = v.toString(16); out += hx.length === 1 ? "0" + hx : hx; } return out; } function hexToHsl(hex) { var clean = String(hex).replace(/[^0-9a-fA-F]/g, ""); if (clean.length !== 6) return null; var r = parseInt(clean.slice(0, 2), 16) / 255; var g = parseInt(clean.slice(2, 4), 16) / 255; var b = parseInt(clean.slice(4, 6), 16) / 255; var max = Math.max(r, g, b); var min = Math.min(r, g, b); var l = (max + min) / 2; var h = 0; var s = 0; if (max !== min) { var d = max - min; s = l > 0.5 ? d / (2 - max - min) : d / (max + min); if (max === r) h = (g - b) / d + (g < b ? 6 : 0); else if (max === g) h = (b - r) / d + 2; else h = (r - g) / d + 4; h = h * 60; } return { h: Math.round(h), s: Math.round(s * 1000) / 10, l: Math.round(l * 1000) / 10, }; } function ensureFontLoaded(family, id, url) { var has = false; try { document.fonts.forEach(function (f) { if (String(f.family).replace(/["']/g, "").trim() === family) { has = true; } }); } catch (e) { has = false; } if (has || document.getElementById(id)) return; var head = document.head || root; if (!document.getElementById(PRECONNECT_ID)) { ["https://fonts.googleapis.com", "https://fonts.gstatic.com"].forEach( function (href, i) { var pc = document.createElement("link"); if (i === 0) pc.id = PRECONNECT_ID; pc.rel = "preconnect"; pc.href = href; if (i === 1) pc.crossOrigin = "anonymous"; head.appendChild(pc); }, ); } var link = document.createElement("link"); link.id = id; link.rel = "stylesheet"; link.href = url; head.appendChild(link); } function applyTypography() { var css = ""; if (typo.pairing === "plex") { ensureFontLoaded("IBM Plex Sans", PLEX_SANS_CSS_ID, PLEX_SANS_URL); ensureFontLoaded("IBM Plex Mono", PLEX_MONO_CSS_ID, PLEX_MONO_URL); css += 'body, .font-sans { font-family: "IBM Plex Sans", ui-sans-serif, system-ui, sans-serif !important; }\n'; css += '.font-mono, code, pre, kbd, samp { font-family: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, monospace !important; }\n'; } else if (typo.pairing === "inter") { ensureFontLoaded("Inter", INTER_CSS_ID, INTER_URL); css += 'body, .font-sans { font-family: "Inter", ui-sans-serif, system-ui, sans-serif !important; }\n'; } var pairing = null; PAIRINGS.forEach(function (p) { if (p.val === typo.pairing) pairing = p; }); if (pairing && pairing.bold) { root.style.setProperty("--font-weight-bold", pairing.bold); } else { root.style.removeProperty("--font-weight-bold"); } updateBoldHint(); TYPE_TOKENS.forEach(function (t) { var decls = []; if (typoSizeChanged(t)) { decls.push( "font-size: " + fmtRem(typo.sizes[t.key]) + "rem !important", ); } if (typoWeightChanged(t)) { decls.push("font-weight: " + typo.weights[t.key] + " !important"); } if (decls.length) { css += ".text-" + t.key + " { " + decls.join("; ") + "; }\n"; } }); var tag = document.getElementById(TYPO_STYLE_ID); if (!css) { if (tag) tag.remove(); return; } if (!tag) { tag = document.createElement("style"); tag.id = TYPO_STYLE_ID; (document.head || root).appendChild(tag); } tag.textContent = css; } function save() { var out = {}; var any = false; ALL.forEach(function (t) { if (isChanged(t)) { out[t.key] = state[t.key]; any = true; } }); if (typo.pairing !== "default") { out.typo = out.typo || {}; out.typo.pairing = typo.pairing; any = true; } TYPE_TOKENS.forEach(function (t) { if (typoSizeChanged(t)) { out.typo = out.typo || {}; out.typo.sizes = out.typo.sizes || {}; out.typo.sizes[t.key] = typo.sizes[t.key]; any = true; } if (typoWeightChanged(t)) { out.typo = out.typo || {}; out.typo.weights = out.typo.weights || {}; out.typo.weights[t.key] = typo.weights[t.key]; any = true; } }); var cur = {}; TEXT_TIERS.forEach(function (t) { if (isTextChanged(t)) cur[t.key] = textState[t.key]; }); textColorsStore[MODE] = cur; ["dark", "light"].forEach(function (m) { if (Object.keys(textColorsStore[m]).length) { out.textColors = out.textColors || {}; out.textColors[m] = textColorsStore[m]; any = true; } }); var curAcc = {}; ACCENT_TIERS.forEach(function (t) { if (isAccentChanged(t)) { var a = accentState[t.key]; curAcc[t.key] = { h: a.h, s: a.s, l: a.l }; } }); accentColorsStore[MODE] = curAcc; ["dark", "light"].forEach(function (m) { if (Object.keys(accentColorsStore[m]).length) { out.accentColors = out.accentColors || {}; out.accentColors[m] = accentColorsStore[m]; any = true; } }); if (ui.width !== UI_DEFAULT_WIDTH || ui.collapsed) { out.ui = { width: ui.width, collapsed: ui.collapsed }; any = true; } try { if (any) localStorage.setItem(LS_KEY, JSON.stringify(out)); else localStorage.removeItem(LS_KEY); } catch (e) { } } (function () { var saved = null; try { saved = JSON.parse(localStorage.getItem(LS_KEY) || "null"); } catch (e) { saved = null; } if (saved && typeof saved === "object") { ALL.forEach(function (t) { var v = saved[t.key]; if (typeof v === "number" && isFinite(v)) { state[t.key] = Math.min(30, Math.max(0, v)); applyTier(t); } }); var tp = saved.typo; if (tp && typeof tp === "object") { var pv = tp.pairing === "system" ? "default" : tp.pairing; if (pv === "inter" || pv === "plex") typo.pairing = pv; if (tp.sizes && typeof tp.sizes === "object") { TYPE_TOKENS.forEach(function (t) { var v = tp.sizes[t.key]; if (typeof v === "number" && isFinite(v)) { if (v > 8) v = v / 16; typo.sizes[t.key] = Math.min(2.5, Math.max(0.5, v)); } }); } if (tp.weights && typeof tp.weights === "object") { TYPE_TOKENS.forEach(function (t) { var v = tp.weights[t.key]; if (WEIGHTS.indexOf(v) !== -1) typo.weights[t.key] = v; }); } applyTypography(); } var tc = saved.textColors; if (tc && typeof tc === "object") { ["dark", "light"].forEach(function (m) { if (tc[m] && typeof tc[m] === "object") { TEXT_TIERS.forEach(function (t) { var v = tc[m][t.key]; if (typeof v === "number" && isFinite(v)) { textColorsStore[m][t.key] = Math.min(100, Math.max(0, v)); } }); } }); TEXT_TIERS.forEach(function (t) { var v = textColorsStore[MODE][t.key]; if (typeof v === "number") { textState[t.key] = v; applyTextTier(t); } }); } var ac = saved.accentColors; if (ac && typeof ac === "object") { ["dark", "light"].forEach(function (m) { if (ac[m] && typeof ac[m] === "object") { ACCENT_TIERS.forEach(function (t) { var v = ac[m][t.key]; if ( v && typeof v === "object" && typeof v.h === "number" && isFinite(v.h) && typeof v.s === "number" && isFinite(v.s) && typeof v.l === "number" && isFinite(v.l) ) { accentColorsStore[m][t.key] = { h: Math.min(360, Math.max(0, v.h)), s: Math.min(100, Math.max(0, v.s)), l: Math.min(100, Math.max(0, v.l)), }; } }); } }); ACCENT_TIERS.forEach(function (t) { var v = accentColorsStore[MODE][t.key]; if (v) { accentState[t.key] = { h: v.h, s: v.s, l: v.l }; applyAccent(t); } }); } var u = saved.ui; if (u && typeof u === "object") { if (typeof u.width === "number" && isFinite(u.width)) { ui.width = Math.round( Math.min(UI_MAX_WIDTH, Math.max(UI_MIN_WIDTH, u.width)), ); } if (u.collapsed === true) ui.collapsed = true; } } })(); var MONO = "ui-monospace, SFMono-Regular, Menlo, Consolas, monospace"; var inputs = {}; var typoInputs = {}; var textInputs = {}; var accentInputs = {}; var pairingRadios = {}; var gapEls = []; var boldHint = null; function updateBoldHint() { if (!boldHint) return; var v = ""; try { v = ( root.style.getPropertyValue("--font-weight-bold") || getComputedStyle(root).getPropertyValue("--font-weight-bold") ).trim(); } catch (e) { v = ""; } boldHint.textContent = "bold: " + (v || "default"); } var panel = document.createElement("div"); panel.id = PANEL_ID; panel.style.cssText = "position:fixed;top:0;right:0;bottom:0;width:" + ui.width + "px;z-index:2147483647;" + "display:flex;flex-direction:column;" + "background:rgb(28,28,30);color:rgb(238,238,238);border-left:1px solid rgb(60,60,66);" + "box-shadow:-8px 0 28px rgba(0,0,0,0.4);" + "font:12px/1.4 " + MONO + ";user-select:none;-webkit-user-select:none;"; var resizeHandle = document.createElement("div"); resizeHandle.title = "drag to resize"; resizeHandle.style.cssText = "position:absolute;left:0;top:0;bottom:0;width:5px;cursor:ew-resize;"; resizeHandle.addEventListener("pointerdown", function (e) { e.preventDefault(); function move(ev) { var wpx = Math.round( Math.min( UI_MAX_WIDTH, Math.max(UI_MIN_WIDTH, window.innerWidth - ev.clientX), ), ); ui.width = wpx; panel.style.width = wpx + "px"; } function up() { window.removeEventListener("pointermove", move); window.removeEventListener("pointerup", up); save(); } window.addEventListener("pointermove", move); window.addEventListener("pointerup", up); }); panel.appendChild(resizeHandle); var header = document.createElement("div"); header.style.cssText = "display:flex;flex:none;align-items:center;gap:6px;padding:7px 10px;" + "border-bottom:1px solid rgb(60,60,66);"; var collapseBtn = document.createElement("button"); collapseBtn.type = "button"; collapseBtn.textContent = String.fromCharCode(187); collapseBtn.title = "Collapse to rail"; collapseBtn.style.cssText = "background:none;border:none;color:rgb(170,170,178);font-size:13px;line-height:1;" + "font-family:" + MONO + ";cursor:pointer;padding:0 2px;margin:0;"; var title = document.createElement("span"); title.textContent = "theme lab"; title.style.cssText = "flex:1;font-weight:600;letter-spacing:0.02em;color:rgb(238,238,238);"; var closeBtn = document.createElement("button"); closeBtn.type = "button"; closeBtn.textContent = String.fromCharCode(215); closeBtn.title = "Close panel (keeps overrides applied)"; closeBtn.style.cssText = "background:none;border:none;color:rgb(170,170,178);font-size:16px;line-height:1;" + "font-family:" + MONO + ";cursor:pointer;padding:0 2px;margin:0;"; closeBtn.addEventListener("click", function () { modeObserver.disconnect(); panel.remove(); }); header.appendChild(collapseBtn); header.appendChild(title); header.appendChild(closeBtn); panel.appendChild(header); var body = document.createElement("div"); body.style.cssText = "padding:8px 10px 10px;flex:1;min-height:0;overflow-y:auto;"; panel.appendChild(body); var rail = document.createElement("button"); rail.type = "button"; rail.title = "Expand theme lab"; rail.textContent = "theme lab"; rail.style.cssText = "display:none;flex:1;align-items:center;justify-content:center;" + "background:none;border:none;color:rgb(198,198,205);cursor:pointer;" + "writing-mode:vertical-rl;font:600 11px " + MONO + ";" + "letter-spacing:0.08em;padding:10px 0;margin:0;"; panel.appendChild(rail); function setCollapsed(c) { ui.collapsed = c; if (c) { panel.style.width = "28px"; resizeHandle.style.display = "none"; header.style.display = "none"; body.style.display = "none"; footer.style.display = "none"; rail.style.display = "flex"; } else { panel.style.width = ui.width + "px"; resizeHandle.style.display = ""; header.style.display = "flex"; body.style.display = ""; footer.style.display = ""; rail.style.display = "none"; } save(); } collapseBtn.addEventListener("click", function () { setCollapsed(true); }); rail.addEventListener("click", function () { setCollapsed(false); }); function makeSection(titleText, open) { var d = document.createElement("details"); if (open) d.open = true; var s = document.createElement("summary"); s.textContent = titleText; s.style.cssText = "cursor:pointer;color:rgb(205,205,212);font-size:11px;font-weight:600;" + "letter-spacing:0.03em;margin:2px 0 3px;"; d.appendChild(s); return d; } function makeSwatch(titleText, getHex, onPick) { var btn = document.createElement("button"); btn.type = "button"; btn.title = titleText; btn.style.cssText = "width:18px;height:18px;flex:none;padding:0;margin:0;" + "border:1px solid rgb(120,120,126);border-radius:4px;cursor:pointer;"; var picker = document.createElement("input"); picker.type = "color"; picker.style.cssText = "position:absolute;width:0;height:0;opacity:0;border:none;padding:0;"; btn.addEventListener("click", function () { picker.value = getHex(); picker.click(); }); picker.addEventListener("input", function () { var hsl = hexToHsl(picker.value); if (hsl) onPick(hsl); }); return { btn: btn, picker: picker, setFill: function (hex) { btn.style.background = hex; }, }; } function makeReadout() { var r = document.createElement("span"); r.title = "current applied value (globals.css format)"; r.style.cssText = "display:block;margin:-2px 0 2px;padding-left:26px;font-family:" + MONO + ";font-size:9px;line-height:1.2;color:rgb(125,125,133);"; return r; } function makeRow(t) { var row = document.createElement("div"); row.style.cssText = "display:flex;align-items:center;gap:6px;margin:2px 0;"; var label = document.createElement("span"); label.textContent = t.label; label.title = t.props.join(", "); label.style.cssText = "width:56px;flex:none;color:rgb(198,198,205);white-space:nowrap;" + "overflow:hidden;text-overflow:ellipsis;font-size:11px;"; var slider = document.createElement("input"); slider.type = "range"; slider.min = "0"; slider.max = "30"; slider.step = "0.1"; slider.style.cssText = "flex:1;min-width:0;margin:0;accent-color:rgb(150,140,255);cursor:pointer;"; var num = document.createElement("input"); num.type = "number"; num.min = "0"; num.max = "30"; num.step = "0.1"; num.style.cssText = "width:50px;flex:none;background:rgb(44,44,48);color:rgb(238,238,238);" + "border:1px solid rgb(72,72,78);border-radius:4px;padding:1px 4px;" + "font-family:" + MONO + ";font-size:11px;user-select:text;-webkit-user-select:text;"; var swatch = makeSwatch( t.props.join(", ") + " - picker applies lightness only (hue/saturation are fixed by design for surfaces)", function () { var b = baselineHsl(t.props[0]); return hslToHex(b.h, b.s, state[t.key]); }, function (hsl) { slider.value = String(Math.min(30, Math.max(0, hsl.l))); num.value = fmtNum(hsl.l); commit(hsl.l); }, ); var readout = makeReadout(); function commit(v) { if (!isFinite(v)) return; v = Math.min(30, Math.max(0, v)); state[t.key] = v; applyTier(t); save(); updateGaps(); refreshMeta(t); } slider.addEventListener("input", function () { var v = parseFloat(slider.value); num.value = fmtNum(v); commit(v); }); num.addEventListener("input", function () { var v = parseFloat(num.value); if (!isFinite(v)) return; slider.value = String(Math.min(30, Math.max(0, v))); commit(v); }); num.addEventListener("change", function () { refresh(t); }); row.appendChild(swatch.btn); row.appendChild(swatch.picker); row.appendChild(label); row.appendChild(slider); row.appendChild(num); inputs[t.key] = { slider: slider, num: num, swatch: swatch, label: label, readout: readout, }; var wrap = document.createElement("div"); wrap.appendChild(row); wrap.appendChild(readout); return wrap; } function refreshMeta(t) { var el = inputs[t.key]; if (!el) return; var b = baselineHsl(t.props[0]); el.swatch.setFill(hslToHex(b.h, b.s, state[t.key])); el.readout.textContent = tripletFor(t.props[0], state[t.key]); el.label.title = t.props.join(", ") + (isChanged(t) ? "\ncode: " + tripletFor(t.props[0], baseline[t.props[0]].l) : ""); } function refresh(t) { var el = inputs[t.key]; if (!el) return; el.slider.value = String(state[t.key]); el.num.value = fmtNum(state[t.key]); refreshMeta(t); } var ladderSection = makeSection("surface ladder", true); body.appendChild(ladderSection); TIERS.forEach(function (t, i) { ladderSection.appendChild(makeRow(t)); if (i < TIERS.length - 1) { var gap = document.createElement("div"); gap.style.cssText = "text-align:right;font-family:" + MONO + ";font-size:10px;line-height:1.1;" + "padding:0 58px 0 0;margin:-1px 0;color:rgb(125,125,133);"; ladderSection.appendChild(gap); gapEls.push(gap); } }); function updateGaps() { gapEls.forEach(function (el, i) { var below = state[TIERS[i].key]; var above = state[TIERS[i + 1].key]; var gap = above - below; var txt = (gap >= 0 ? "+" : "") + fmtNum(gap) + "pp"; if (gap < 0) { el.textContent = txt + " order!"; el.style.color = "rgb(255,118,118)"; el.style.fontWeight = "700"; } else if (gap < 1.5) { el.textContent = txt + " tight"; el.style.color = "rgb(250,200,90)"; el.style.fontWeight = "600"; } else { el.textContent = txt; el.style.color = "rgb(125,125,133)"; el.style.fontWeight = "400"; } }); updateTextContrast(); updateAccentContrast(); } var extrasDetails = document.createElement("details"); var extrasSummary = document.createElement("summary"); extrasSummary.textContent = "secondary tokens"; extrasSummary.style.cssText = "cursor:pointer;color:rgb(160,160,168);font-size:11px;margin:6px 0 2px;"; extrasDetails.appendChild(extrasSummary); EXTRAS.forEach(function (t) { extrasDetails.appendChild(makeRow(t)); }); ladderSection.appendChild(extrasDetails); function makeBtn(text, titleText, onClick) { var b = document.createElement("button"); b.type = "button"; b.textContent = text; if (titleText) b.title = titleText; b.style.cssText = "background:rgb(56,56,62);color:rgb(230,230,236);border:1px solid rgb(78,78,84);" + "border-radius:5px;padding:3px 7px;font-size:11px;font-family:" + MONO + ";cursor:pointer;flex:1;min-width:0;"; b.addEventListener("click", onClick); return b; } var presetRow = document.createElement("div"); presetRow.style.cssText = "display:flex;gap:5px;margin-top:6px;"; PRESETS.forEach(function (p) { presetRow.appendChild( makeBtn(p.name, "Apply preset: " + JSON.stringify(p.vals), function () { applyPreset(p.vals); }), ); }); ladderSection.appendChild(presetRow); function applyPreset(vals) { TIERS.forEach(function (t) { if (typeof vals[t.key] === "number") { state[t.key] = vals[t.key]; applyTier(t); refresh(t); } }); save(); updateGaps(); } var typoSection = makeSection("typography", false); typoSection.style.marginTop = "6px"; body.appendChild(typoSection); var pairingRow = document.createElement("div"); pairingRow.style.cssText = "display:flex;flex-wrap:wrap;align-items:center;gap:4px 8px;margin:2px 0 5px;" + "font-size:11px;color:rgb(198,198,205);"; PAIRINGS.forEach(function (opt) { var lab = document.createElement("label"); lab.style.cssText = "display:flex;align-items:center;gap:4px;cursor:pointer;"; var r = document.createElement("input"); r.type = "radio"; r.name = "theme-lab-pairing"; r.value = opt.val; r.style.cssText = "margin:0;accent-color:rgb(150,140,255);cursor:pointer;"; r.addEventListener("change", function () { if (!r.checked) return; typo.pairing = opt.val; applyTypography(); save(); }); lab.appendChild(r); lab.appendChild(document.createTextNode(opt.label)); pairingRadios[opt.val] = r; pairingRow.appendChild(lab); }); boldHint = document.createElement("span"); boldHint.title = "applied --font-weight-bold (forced per pairing; pairing selection wins)"; boldHint.style.cssText = "margin-left:auto;color:rgb(130,130,138);font-family:" + MONO + ";font-size:9px;white-space:nowrap;"; pairingRow.appendChild(boldHint); typoSection.appendChild(pairingRow); function markTypo(t) { var el = typoInputs[t.key]; if (!el) return; var changed = typoRowChanged(t); el.label.style.color = changed ? "rgb(178,168,255)" : "rgb(198,198,205)"; el.label.style.fontWeight = changed ? "600" : "400"; } function refreshTypo(t) { var el = typoInputs[t.key]; if (!el) return; el.slider.value = String(typo.sizes[t.key]); el.num.value = fmtRem(typo.sizes[t.key]); el.pxHint.textContent = fmtPx(typo.sizes[t.key]) + "px"; el.weight.value = typo.weights[t.key] != null ? String(typo.weights[t.key]) : ""; markTypo(t); } function makeTypeRow(t) { var row = document.createElement("div"); row.style.cssText = "display:flex;align-items:center;gap:6px;margin:2px 0;"; var label = document.createElement("span"); label.textContent = t.key + " (" + fmtRem(t.def) + ")"; label.title = "--text-" + t.key + " (default " + fmtRem(t.def) + "rem)"; label.style.cssText = "width:68px;flex:none;color:rgb(198,198,205);white-space:nowrap;" + "overflow:hidden;text-overflow:ellipsis;font-size:11px;"; var slider = document.createElement("input"); slider.type = "range"; slider.min = "0.5"; slider.max = "2.5"; slider.step = "0.025"; slider.style.cssText = "flex:1;min-width:0;margin:0;accent-color:rgb(150,140,255);cursor:pointer;"; var num = document.createElement("input"); num.type = "number"; num.min = "0.5"; num.max = "2.5"; num.step = "0.025"; num.style.cssText = "width:50px;flex:none;background:rgb(44,44,48);color:rgb(238,238,238);" + "border:1px solid rgb(72,72,78);border-radius:4px;padding:1px 4px;" + "font-family:" + MONO + ";font-size:11px;user-select:text;-webkit-user-select:text;"; var pxHint = document.createElement("span"); pxHint.style.cssText = "width:36px;flex:none;color:rgb(130,130,138);font-family:" + MONO + ";font-size:9px;text-align:right;white-space:nowrap;"; var weight = document.createElement("select"); weight.title = "font-weight override for .text-" + t.key; weight.style.cssText = "width:44px;flex:none;background:rgb(44,44,48);color:rgb(238,238,238);" + "border:1px solid rgb(72,72,78);border-radius:4px;padding:1px 2px;" + "font-family:" + MONO + ";font-size:10px;cursor:pointer;"; var defOpt = document.createElement("option"); defOpt.value = ""; defOpt.textContent = String.fromCharCode(8211); weight.appendChild(defOpt); WEIGHTS.forEach(function (wv) { var o = document.createElement("option"); o.value = String(wv); o.textContent = String(wv); weight.appendChild(o); }); weight.addEventListener("change", function () { typo.weights[t.key] = weight.value === "" ? null : parseInt(weight.value, 10); applyTypography(); save(); markTypo(t); }); function commit(v) { if (!isFinite(v)) return; v = Math.min(2.5, Math.max(0.5, v)); typo.sizes[t.key] = v; pxHint.textContent = fmtPx(v) + "px"; applyTypography(); save(); markTypo(t); } slider.addEventListener("input", function () { var v = parseFloat(slider.value); num.value = fmtRem(v); commit(v); }); num.addEventListener("input", function () { var v = parseFloat(num.value); if (!isFinite(v)) return; slider.value = String(Math.min(2.5, Math.max(0.5, v))); commit(v); }); num.addEventListener("change", function () { refreshTypo(t); }); row.appendChild(label); row.appendChild(slider); row.appendChild(num); row.appendChild(pxHint); row.appendChild(weight); typoInputs[t.key] = { slider: slider, num: num, label: label, pxHint: pxHint, weight: weight, defOpt: defOpt, }; return row; } TYPE_TOKENS.forEach(function (t) { typoSection.appendChild(makeTypeRow(t)); }); function sampleWeights() { var DASH = String.fromCharCode(8211); var TIMES = String.fromCharCode(215); TYPE_TOKENS.forEach(function (t) { var ui = typoInputs[t.key]; if (!ui) return; var tally = {}; var sampled = 0; try { var els = document.querySelectorAll(".text-" + t.key); for (var i = 0; i < els.length && sampled < 200; i++) { if (panel.contains(els[i])) continue; var fw = getComputedStyle(els[i]).fontWeight; if (!fw) continue; tally[fw] = (tally[fw] || 0) + 1; sampled++; } } catch (e) { tally = {}; sampled = 0; } var baseTitle = "font-weight override for .text-" + t.key; var keys = Object.keys(tally); if (!keys.length) { ui.defOpt.textContent = DASH; ui.weight.title = baseTitle + " (no .text-" + t.key + " on page)"; return; } keys.sort(function (a, b) { return tally[b] - tally[a]; }); var dominant = keys[0]; var mixed = keys.length > 1; ui.defOpt.textContent = DASH + " (" + dominant + (mixed ? "*" : "") + ")"; var parts = keys.map(function (k) { return k + " " + TIMES + tally[k]; }); ui.weight.title = baseTitle + " - sampled: " + parts.join(", "); }); } var rescanBtn = document.createElement("button"); rescanBtn.type = "button"; rescanBtn.textContent = "rescan"; rescanBtn.title = "Re-sample rendered font-weights per .text-* class"; rescanBtn.style.cssText = "background:none;border:none;color:rgb(150,140,255);font-size:10px;" + "font-family:" + MONO + ";cursor:pointer;padding:0;margin-left:8px;text-decoration:underline;"; rescanBtn.addEventListener("click", function (e) { e.preventDefault(); e.stopPropagation(); sampleWeights(); }); typoSection.querySelector("summary").appendChild(rescanBtn); var textSection = makeSection("text colors", false); textSection.style.marginTop = "6px"; body.appendChild(textSection); function contrastColor(ratio) { if (ratio >= 4.5) return "rgb(120,220,140)"; if (ratio >= 3) return "rgb(250,200,90)"; return "rgb(255,118,118)"; } function updateTextContrast() { TEXT_TIERS.forEach(function (t) { var el = textInputs[t.key]; if (!el) return; var ratio = textContrast(t); el.ratio.textContent = fmtNum(ratio) + ":1"; el.ratio.style.color = contrastColor(ratio); }); } function textTitle(t) { return ( t.prop + " vs " + t.vs + ", sampled in " + MODE + " mode (" + MODE + ")" ); } function refreshTextMeta(t) { var el = textInputs[t.key]; if (!el) return; var b = baselineHsl(t.prop); el.swatch.setFill(hslToHex(b.h, b.s, textState[t.key])); el.readout.textContent = tripletFor(t.prop, textState[t.key]); el.label.title = textTitle(t) + (isTextChanged(t) ? "\ncode: " + tripletFor(t.prop, baseline[t.prop].l) : ""); } function refreshText(t) { var el = textInputs[t.key]; if (!el) return; el.slider.value = String(textState[t.key]); el.num.value = fmtNum(textState[t.key]); refreshTextMeta(t); } function makeTextRow(t) { var row = document.createElement("div"); row.style.cssText = "display:flex;align-items:center;gap:6px;margin:2px 0;"; var label = document.createElement("span"); label.textContent = t.label; label.title = textTitle(t); label.style.cssText = "width:68px;flex:none;color:rgb(198,198,205);white-space:nowrap;" + "overflow:hidden;text-overflow:ellipsis;font-size:11px;"; var slider = document.createElement("input"); slider.type = "range"; slider.min = "0"; slider.max = "100"; slider.step = "0.1"; slider.style.cssText = "flex:1;min-width:0;margin:0;accent-color:rgb(150,140,255);cursor:pointer;"; var num = document.createElement("input"); num.type = "number"; num.min = "0"; num.max = "100"; num.step = "0.1"; num.style.cssText = "width:46px;flex:none;background:rgb(44,44,48);color:rgb(238,238,238);" + "border:1px solid rgb(72,72,78);border-radius:4px;padding:1px 4px;" + "font-family:" + MONO + ";font-size:11px;user-select:text;-webkit-user-select:text;"; var ratio = document.createElement("span"); ratio.title = "WCAG contrast ratio " + t.prop + " vs " + t.vs; ratio.style.cssText = "width:36px;flex:none;font-family:" + MONO + ";font-size:9px;text-align:right;white-space:nowrap;"; var swatch = makeSwatch( t.prop + " - picker applies lightness only (hue/saturation come from the stylesheet baseline)", function () { var b = baselineHsl(t.prop); return hslToHex(b.h, b.s, textState[t.key]); }, function (hsl) { slider.value = String(Math.min(100, Math.max(0, hsl.l))); num.value = fmtNum(hsl.l); commit(hsl.l); }, ); var readout = makeReadout(); function commit(v) { if (!isFinite(v)) return; v = Math.min(100, Math.max(0, v)); textState[t.key] = v; applyTextTier(t); save(); updateTextContrast(); refreshTextMeta(t); } slider.addEventListener("input", function () { var v = parseFloat(slider.value); num.value = fmtNum(v); commit(v); }); num.addEventListener("input", function () { var v = parseFloat(num.value); if (!isFinite(v)) return; slider.value = String(Math.min(100, Math.max(0, v))); commit(v); }); num.addEventListener("change", function () { refreshText(t); }); row.appendChild(swatch.btn); row.appendChild(swatch.picker); row.appendChild(label); row.appendChild(slider); row.appendChild(num); row.appendChild(ratio); textInputs[t.key] = { slider: slider, num: num, label: label, ratio: ratio, swatch: swatch, readout: readout, }; var wrap = document.createElement("div"); wrap.appendChild(row); wrap.appendChild(readout); return wrap; } TEXT_TIERS.forEach(function (t) { textSection.appendChild(makeTextRow(t)); }); var accentSection = makeSection("accent colors", false); accentSection.style.marginTop = "6px"; body.appendChild(accentSection); function updateAccentContrast() { ACCENT_TIERS.forEach(function (t) { var el = accentInputs[t.key]; if (!el || !t.vsProp) return; var ratio = contrastRatio(computedHsl(t.prop), computedHsl(t.vsProp)); el.ratio.textContent = fmtNum(ratio) + ":1"; el.ratio.style.color = contrastColor(ratio); }); } function accentSliderBg(a) { return ( "linear-gradient(to right, rgb(0,0,0), " + hslToHex(a.h, a.s, 50) + ", rgb(255,255,255))" ); } function accentTitle(t) { return ( t.prop + (t.vsProp ? " vs " + t.vsProp : " (fill)") + ", sampled in " + MODE + " mode (" + MODE + ")" ); } function refreshAccentMeta(t) { var el = accentInputs[t.key]; if (!el) return; var a = accentState[t.key]; el.slider.style.background = accentSliderBg(a); el.swatch.setFill(hslToHex(a.h, a.s, a.l)); el.readout.textContent = hslTripletStr(a); el.label.title = accentTitle(t) + (isAccentChanged(t) ? "\ncode: " + hslTripletStr(baselineHsl(t.prop)) : ""); } function refreshAccent(t) { var el = accentInputs[t.key]; if (!el) return; var a = accentState[t.key]; el.h.value = fmtNum(a.h); el.s.value = fmtNum(a.s); el.l.value = fmtNum(a.l); el.slider.value = String(a.l); refreshAccentMeta(t); } function makeAccentRow(t) { var row = document.createElement("div"); row.style.cssText = "display:flex;align-items:center;gap:4px;margin:2px 0;"; var label = document.createElement("span"); label.textContent = t.label; label.title = accentTitle(t); label.style.cssText = "width:52px;flex:none;color:rgb(198,198,205);white-space:nowrap;" + "overflow:hidden;text-overflow:ellipsis;font-size:10px;"; var slider = document.createElement("input"); slider.type = "range"; slider.min = "0"; slider.max = "100"; slider.step = "0.1"; slider.title = "lightness"; slider.style.cssText = "flex:1;min-width:24px;margin:0;height:10px;border-radius:5px;" + "accent-color:rgb(150,140,255);cursor:pointer;"; function makeNum(min, max, step, titleText, width) { var n = document.createElement("input"); n.type = "number"; n.min = String(min); n.max = String(max); n.step = String(step); n.title = titleText; n.style.cssText = "width:" + width + "px;flex:none;background:rgb(44,44,48);color:rgb(238,238,238);" + "border:1px solid rgb(72,72,78);border-radius:4px;padding:1px 3px;" + "font-family:" + MONO + ";font-size:10px;user-select:text;-webkit-user-select:text;"; return n; } var hNum = makeNum(0, 360, 1, "hue (0-360)", 34); var sNum = makeNum(0, 100, 0.5, "saturation (0-100)", 34); var lNum = makeNum(0, 100, 0.1, "lightness (0-100)", 38); var ratio = document.createElement("span"); if (t.vsProp) { ratio.title = "WCAG contrast ratio " + t.prop + " vs " + t.vsProp; } ratio.style.cssText = "width:28px;flex:none;font-family:" + MONO + ";font-size:9px;text-align:right;white-space:nowrap;"; var swatch = makeSwatch( t.prop + " - pick a color (Chrome's picker includes an eyedropper)", function () { var a = accentState[t.key]; return hslToHex(a.h, a.s, a.l); }, function (hsl) { accentState[t.key] = { h: hsl.h, s: hsl.s, l: hsl.l }; refreshAccent(t); applyAccent(t); save(); updateAccentContrast(); }, ); var readout = makeReadout(); function commitPart(part, v, min, max) { if (!isFinite(v)) return; accentState[t.key][part] = Math.min(max, Math.max(min, v)); applyAccent(t); save(); updateAccentContrast(); refreshAccentMeta(t); } slider.addEventListener("input", function () { var v = parseFloat(slider.value); lNum.value = fmtNum(v); commitPart("l", v, 0, 100); }); hNum.addEventListener("input", function () { commitPart("h", parseFloat(hNum.value), 0, 360); }); sNum.addEventListener("input", function () { commitPart("s", parseFloat(sNum.value), 0, 100); }); lNum.addEventListener("input", function () { var v = parseFloat(lNum.value); if (!isFinite(v)) return; slider.value = String(Math.min(100, Math.max(0, v))); commitPart("l", v, 0, 100); }); [hNum, sNum, lNum].forEach(function (n) { n.addEventListener("change", function () { refreshAccent(t); }); }); row.appendChild(swatch.btn); row.appendChild(swatch.picker); row.appendChild(label); row.appendChild(slider); row.appendChild(hNum); row.appendChild(sNum); row.appendChild(lNum); row.appendChild(ratio); accentInputs[t.key] = { slider: slider, h: hNum, s: sNum, l: lNum, label: label, ratio: ratio, swatch: swatch, readout: readout, }; var wrap = document.createElement("div"); wrap.appendChild(row); wrap.appendChild(readout); return wrap; } ACCENT_TIERS.forEach(function (t) { accentSection.appendChild(makeAccentRow(t)); }); function resetAll() { ALL.forEach(function (t) { t.props.forEach(function (prop) { root.style.removeProperty(prop); }); state[t.key] = baseline[t.props[0]].l; refresh(t); }); typo.pairing = "default"; TYPE_TOKENS.forEach(function (t) { typo.sizes[t.key] = t.def; typo.weights[t.key] = null; refreshTypo(t); }); pairingRadios["default"].checked = true; root.style.removeProperty("--font-weight-bold"); updateBoldHint(); var tag = document.getElementById(TYPO_STYLE_ID); if (tag) tag.remove(); TEXT_TIERS.forEach(function (t) { root.style.removeProperty(t.prop); textState[t.key] = baseline[t.prop].l; refreshText(t); }); textColorsStore = { dark: {}, light: {} }; ACCENT_TIERS.forEach(function (t) { root.style.removeProperty(t.prop); accentState[t.key] = baselineHsl(t.prop); refreshAccent(t); }); accentColorsStore = { dark: {}, light: {} }; try { localStorage.removeItem(LS_KEY); } catch (e) { } updateGaps(); } function flash(btn, text) { var orig = btn.textContent; btn.textContent = text; setTimeout(function () { btn.textContent = orig; }, 1200); } function fallbackCopy(text) { var ta = document.createElement("textarea"); ta.value = text; ta.style.cssText = "position:fixed;top:0;left:0;opacity:0;"; document.body.appendChild(ta); ta.select(); try { document.execCommand("copy"); } catch (e) { } ta.remove(); } function copyCss(btn) { var lines = []; ALL.forEach(function (t) { if (isChanged(t)) { t.props.forEach(function (prop) { lines.push(prop + ": " + tripletFor(prop, state[t.key]) + ";"); }); } }); var typoLines = []; TYPE_TOKENS.forEach(function (t) { if (typoSizeChanged(t)) { typoLines.push( "--text-" + t.key + ": " + fmtRem(typo.sizes[t.key]) + "rem;", ); } if (typoWeightChanged(t)) { typoLines.push( CO + " text-" + t.key + ": weight " + typo.weights[t.key] + " " + CC, ); } }); if (typoLines.length || typo.pairing !== "default") { if (lines.length) lines.push(""); lines.push(CO + " typography (Tailwind @theme inline text tokens) " + CC); typoLines.forEach(function (l) { lines.push(l); }); if (typo.pairing !== "default") { PAIRINGS.forEach(function (p) { if (p.val === typo.pairing) { lines.push( CO + " pairing: " + p.label + " (bold " + p.bold + ") " + CC, ); } }); } } var textLines = []; TEXT_TIERS.forEach(function (t) { if (isTextChanged(t)) { textLines.push( t.prop + ": " + tripletFor(t.prop, textState[t.key]) + ";", ); } }); if (textLines.length) { if (lines.length) lines.push(""); lines.push(CO + " text colors (" + MODE + ") " + CC); textLines.forEach(function (l) { lines.push(l); }); } var accentLines = []; ACCENT_TIERS.forEach(function (t) { if (isAccentChanged(t)) { accentLines.push(t.prop + ": " + accentTripletStr(t) + ";"); } }); if (accentLines.length) { if (lines.length) lines.push(""); lines.push(CO + " accent colors (" + MODE + ") " + CC); accentLines.forEach(function (l) { lines.push(l); }); } if (!lines.length) { flash(btn, "no changes"); return; } var text = lines.join("\n"); var done = function () { flash(btn, "copied " + lines.length); }; if (navigator.clipboard && navigator.clipboard.writeText) { navigator.clipboard.writeText(text).then(done, function () { fallbackCopy(text); done(); }); } else { fallbackCopy(text); done(); } } var footer = document.createElement("div"); footer.style.cssText = "flex:none;padding:8px 10px;border-top:1px solid rgb(60,60,66);"; var actionRow = document.createElement("div"); actionRow.style.cssText = "display:flex;gap:5px;"; actionRow.appendChild( makeBtn( "Reset", "Clear all overrides (ladder, typography, text colors, accents) and persisted values", resetAll, ), ); var copyBtn = makeBtn( "Copy CSS", "Copy changed tokens as globals.css lines", function () { copyCss(copyBtn); }, ); actionRow.appendChild(copyBtn); footer.appendChild(actionRow); panel.appendChild(footer); function handleModeFlip() { var newMode = root.classList.contains("dark") ? "dark" : "light"; if (newMode === MODE) return; MODE = newMode; TEXT_TIERS.forEach(function (t) { root.style.removeProperty(t.prop); }); ACCENT_TIERS.forEach(function (t) { root.style.removeProperty(t.prop); }); TEXT_TIERS.forEach(function (t) { captureBaseline(t.prop); var v = textColorsStore[MODE][t.key]; if (typeof v === "number") { textState[t.key] = v; applyTextTier(t); } else { textState[t.key] = baseline[t.prop].l; } refreshText(t); }); ACCENT_TIERS.forEach(function (t) { captureBaseline(t.prop); var v = accentColorsStore[MODE][t.key]; if (v) { accentState[t.key] = { h: v.h, s: v.s, l: v.l }; applyAccent(t); } else { accentState[t.key] = baselineHsl(t.prop); } refreshAccent(t); }); updateTextContrast(); updateAccentContrast(); } var modeObserver = new MutationObserver(handleModeFlip); modeObserver.observe(root, { attributes: true, attributeFilter: ["class"] }); ALL.forEach(refresh); TYPE_TOKENS.forEach(refreshTypo); TEXT_TIERS.forEach(refreshText); ACCENT_TIERS.forEach(refreshAccent); pairingRadios[typo.pairing].checked = true; updateBoldHint(); updateGaps(); sampleWeights(); if (ui.collapsed) setCollapsed(true); (document.body || root).appendChild(panel); })();

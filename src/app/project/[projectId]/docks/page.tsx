import Dock from "./Dock";

type Props = {};

const IntelligencePage = (props: Props) => {
  return (
    <main className="w-full h-screen">
      <Dock />
    </main>
  );
};

export default IntelligencePage;

# Claude Skills

Shared repo skills now live in [`.agents/skills/README.md`](../../.agents/skills/README.md).

Use `CLAUDE.md` to point Claude Code at those neutral skill files with `@`
imports. Keep tool-independent skill content under `.agents/skills/`.
